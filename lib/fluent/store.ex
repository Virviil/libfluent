defmodule Fluent.Store do
  @moduledoc """
  This module hande references to active bundles, that are using during runtimne
  of the program
  """
  def initialize_bundle(assembly, locale, path) do

  end
end
